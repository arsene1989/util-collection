<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration status="INFO" monitorInterval="30" shutdownHook="disable">
    <properties>
        <property name="LOG_HOME">/var/log/util-collection</property>
        <property name="PROJECT_NAME">util-collection</property>
        <!--日志打印格式222-->

        <Property name="log.layout">%-d{yyyy-MM-dd HH:mm:ss,SSS} %highlight{[%p]}{FATAL=red,ERROR=red,
            WARN=yellow,INFO=green,DEBUG=green,TRACE=blue} %style{[%t]}{Bright,Magenta} %style{%.30c.%M(%L)}{cyan} - %m%n</Property>

        <!-- debug 日志文件位置及名称，在rollFile的时候老文件的生成规则 -->
        <property name="DEBUG_FILE_PATH_ING" value="${LOG_HOME}/debug/${PROJECT_NAME}.log"/>
        <property name="DEBUG_FILE_PATH_ED" value="${LOG_HOME}/debug/${PROJECT_NAME}_debug-%d{yyyy-MM-dd}.%i.log"/>

        <!--日志切割的最小单位-->
        <property name="every_file_size">100M</property>
    </properties>
    <!--先定义所有的appender-->
    <appenders>
        <!--这个输出控制台的配置-->
        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${log.layout}"/>
        </console>

        <!-- 这个会打印出所有的info及以下级别的信息，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <!--<RollingFile name="RollingFileInfo" fileName="${LOG_HOME}/${PROJECT_NAME}.log"-->
                     <!--filePattern="${LOG_HOME}/$${date:yyyy-MM}/${PROJECT_NAME}-%d{yyyy-MM-dd}.log">-->
        <RollingFile name="RollingFileInfo" fileName="${LOG_HOME}/${PROJECT_NAME}.log"
                     filePattern="${LOG_HOME}/${PROJECT_NAME}-%d{yyyy-MM-dd}-%i.log">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${log.layout}"/>
            <Policies>
                <TimeBasedTriggeringPolicy  modulate="true"
                                            interval="1"/>
                <SizeBasedTriggeringPolicy size="${every_file_size}"/>
            </Policies>
            <DefaultRolloverStrategy max="90">
                <Delete basePath="${LOG_HOME}/" maxDepth="1">
                    <IfFileName glob="*.log" />
                    <!--!Note: 这里的age必须和filePattern协调, 后者是精确到HH, 这里就要写成xH, xd就不起作用 另外, 数字最好>2,
                        否则可能造成删除的时候, 最近的文件还处于被占用状态,导致删除不成功! -->
                    <IfLastModified age="91d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

        <!--         debug日志文件输出日志信息-->
        <RollingFile name="DebugFile" fileName="${DEBUG_FILE_PATH_ING}" filePattern="${DEBUG_FILE_PATH_ED}">
            <ThresholdFilter level="DEBUG" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${log.layout}"/>
            <Policies>
                <TimeBasedTriggeringPolicy  modulate="true"
                                            interval="1"/>
                <SizeBasedTriggeringPolicy size="500M"/>
            </Policies>
            <DefaultRolloverStrategy max="20">
                <Delete basePath="${LOG_HOME}/debug/" maxDepth="1">
                    <IfFileName glob="*.log" />
                    <!--!Note: 这里的age必须和filePattern协调, 后者是精确到HH, 这里就要写成xH, xd就不起作用 另外, 数字最好>2,
                        否则可能造成删除的时候, 最近的文件还处于被占用状态,导致删除不成功! -->
                    <IfLastModified age="91d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </appenders>
    <loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
        </Root>
    </loggers>

</configuration>
